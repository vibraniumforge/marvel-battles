<h1>Characters#show</h1>
<p>Find me in app/views/characters/show.html.erb</p>

<h1>Show a character</h1>

<p>Name: <%= @character.name %></p>
<p>Alias: <%= @character.alias %></p>
<p>Superpowers:<% @character.superpowers.each do |p|  %>
    <%= link_to p.name, character_path(@character) %>
  <% end %>
  </p>
<br>
<%= link_to "Edit Character", edit_character_path(@character) %>
<br>
<%= link_to "Delete Character", @character, method: :delete, data: { confirm: "Really?" } %>
<br>
<%= link_to "View all Characters", characters_path %><br>